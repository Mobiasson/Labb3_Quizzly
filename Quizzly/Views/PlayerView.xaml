<UserControl x:Class="Quizzly.Views.PlayerView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800" Width="auto" Height="auto" Margin="20" DataContext="{Binding PlayerViewModel}">
	<UserControl.Resources>
		<Style x:Key="AnswerButtonStyle" TargetType="Button">
			<Setter Property="BorderBrush" Value="Gray" />
			<Setter Property="BorderThickness" Value="1" />
			<Setter Property="Padding" Value="12" />
			<Setter Property="FontSize" Value="18" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
							<ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True" />
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsEnabled" Value="False">
								<Setter TargetName="bd" Property="Opacity" Value="0.6" />
							</Trigger>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="bd" Property="Opacity" Value="0.95" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>
	</UserControl.Resources>
	<Grid Background="White">
		<Grid.RowDefinitions>
			<RowDefinition Height="0.3*" />
			<RowDefinition Height="*" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid Grid.Row="0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<TextBlock Grid.Column="1" Text="{Binding TimeRemainingDisplay}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="20" FontWeight="Bold" Margin="8" />
			<Label Grid.Column="2" Content="{Binding QuestionProgress}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="8,0,10,0" FontSize="20" FontWeight="Bold" />
		</Grid>
		<TextBlock Grid.Row="1" Text="{Binding CurrentQuestion.Query}" Width="auto" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" TextWrapping="Wrap" Padding="5" Margin="20" />
		<UniformGrid Grid.Row="2" Rows="2" Columns="2" Margin="10">
			<Button Style="{StaticResource AnswerButtonStyle}" Content="{Binding Answers[0].Text}" Command="{Binding Answers[0].SelectCommand}" CommandParameter="{Binding Answers[0].Text}" Margin="5" Background="{Binding Answers[0].Background}" />
			<Button Style="{StaticResource AnswerButtonStyle}" Content="{Binding Answers[1].Text}" Command="{Binding Answers[1].SelectCommand}" CommandParameter="{Binding Answers[1].Text}" Margin="5" Background="{Binding Answers[1].Background}" />
			<Button Style="{StaticResource AnswerButtonStyle}" Content="{Binding Answers[2].Text}" Command="{Binding Answers[2].SelectCommand}" CommandParameter="{Binding Answers[2].Text}" Margin="5" Background="{Binding Answers[2].Background}" />
			<Button Style="{StaticResource AnswerButtonStyle}" Content="{Binding Answers[3].Text}" Command="{Binding Answers[3].SelectCommand}" CommandParameter="{Binding Answers[3].Text}" Margin="5" Background="{Binding Answers[3].Background}" />
		</UniformGrid>
	</Grid>
</UserControl>
