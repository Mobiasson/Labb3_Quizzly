<UserControl x:Class="Quizzly.Views.PlayerView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800" DataContext="{Binding PlayerViewModel}">
	<Grid Background="White" ShowGridLines="True">
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="2*" />
		</Grid.RowDefinitions>
		<TextBlock Text="{Binding ActivePack.Name}" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" />
		<Button Content="BACK" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="5" Padding="5" Click="Back_Click"/>
		<UniformGrid Grid.Row="1" Rows="2" Columns="2" Margin="20">
			<Button Content="{Binding Answers[0].Text}" Command="{Binding Answers[0].SelectCommand}" Background="LightGray" />
			<Button Content="{Binding Answers[1].Text}" Command="{Binding Answers[1].SelectCommand}" Background="LightGray" />
			<Button Content="{Binding ActivePack.SelectedQuestion.IncorrectAnswers[2], Mode=OneWay}" Command="{Binding Answers[2].SelectCommand}" Background="LightGray" />
			<Button Content="{Binding Answers[3].Text}" Command="{Binding Answers[3].SelectCommand}" Background="LightGray" />
		</UniformGrid>
	</Grid>
</UserControl>
