<UserControl x:Class="Quizzly.Views.PlayerView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800" DataContext="{Binding PlayerViewModel}">
	<Grid Background="White">
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="2*" />
		</Grid.RowDefinitions>
		<TextBlock Text="{Binding CurrentQuestion.Query}" Width="auto" Height="auto" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" TextWrapping="Wrap" Padding="5" Margin="5" />
		<Button Content="BACK" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="5" Padding="5" Click="Back_Click" />
		<UniformGrid Grid.Row="1" Rows="2" Columns="2" Margin="20">
			<Button x:Name="button1_answer" Content="{Binding Answers[0].Text}" Command="{Binding Answers[0].SelectCommand}" CommandParameter="{Binding Answers[0].Text}" Margin="8" Padding="20" FontSize="18" Background="LightGray" />
			<Button x:Name="button2_answer" Content="{Binding Answers[1].Text}" Command="{Binding Answers[1].SelectCommand}" CommandParameter="{Binding Answers[1].Text}" Margin="8" Padding="20" FontSize="18" Background="LightGray" />
			<Button x:Name="button3_answer" Content="{Binding Answers[2].Text}" Command="{Binding Answers[2].SelectCommand}" CommandParameter="{Binding Answers[2].Text}" Margin="8" Padding="20" FontSize="18" Background="LightGray" />
			<Button x:Name="button4_answer" Content="{Binding Answers[3].Text}" Command="{Binding Answers[3].SelectCommand}" CommandParameter="{Binding Answers[3].Text}" Margin="8" Padding="20" FontSize="18" Background="LightGray" />
		</UniformGrid>
	</Grid>
</UserControl>

