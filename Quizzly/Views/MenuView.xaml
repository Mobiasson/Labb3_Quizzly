<UserControl x:Class="Quizzly.Views.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="fa:FontAwesome">
            <Setter Property="Width" Value="auto" />
            <Setter Property="Height" Value="auto" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="HorizontalAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>
    <UserControl.InputBindings>
        <KeyBinding Key="N" Modifiers="Control" Command="{Binding NewPackCommand}" />
        <KeyBinding Key="I" Modifiers="Control" Command="{Binding ImportPackCommand}" />
        <KeyBinding Key="X" Modifiers="Control" Command="{Binding ExitCommand}" />
        <KeyBinding Key="R" Modifiers="Control" Command="{Binding RemoveQuestionCommand}" />
        <KeyBinding Key="P" Modifiers="Control" Command="{Binding SetPackNameCommand}" />
        <KeyBinding Key="O" Modifiers="Control" Command="{Binding PackOptionsCommand}" />
        <KeyBinding Key="D" Modifiers="Control" Command="{Binding DeletePackCommand}" />
        <KeyBinding Key="F11" Command="{Binding FullscreenCommand}" />
    </UserControl.InputBindings>
    <Menu>
        <MenuItem Header="_File">
            <MenuItem Header="_New Question Pack" InputGestureText="Ctrl+N" Command="{Binding NewPackCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="File" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Saved Question Packs" ItemsSource="{Binding Packs}">
                <MenuItem.ItemContainerStyle>
                    <Style TargetType="MenuItem">
                        <Setter Property="Header" Value="{Binding Name}" />
                        <Setter Property="Command" Value="{Binding DataContext.ActivatePackCommand, RelativeSource={RelativeSource AncestorType=Menu}}" />
                        <Setter Property="CommandParameter" Value="{Binding}" />
                    </Style>
                </MenuItem.ItemContainerStyle>
            </MenuItem>
            <Separator />
            <MenuItem Header="_Import Question Pack" InputGestureText="Ctrl+I" Command="{Binding ImportPackCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Download" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="_Delete Pack" InputGestureText="Ctrl+D" Command="{Binding DeletePackCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Trash" />
                </MenuItem.Icon>
            </MenuItem>
            <Separator />
            <MenuItem Header="E_xit" InputGestureText="Ctrl+X" Command="{Binding ExitCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="WindowClose" />
                </MenuItem.Icon>
            </MenuItem>
        </MenuItem>
        <MenuItem Header="_Edit">
            <MenuItem Header="Add Question" Command="{Binding AddQuestionCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Plus" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Remove Question" InputGestureText="Ctrl+R" Command="{Binding RemoveQuestionCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Trash" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Set PackName" InputGestureText="Ctrl+P" Command="{Binding SetPackNameCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Edit" />
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Pack Options" InputGestureText="Ctrl+O" Command="{Binding PackOptionsCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Cogs" />
                </MenuItem.Icon>
            </MenuItem>
        </MenuItem>
        <MenuItem Header="_View">
            <MenuItem Header="Play" Command="{Binding PlayCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="Play" />
                </MenuItem.Icon>
            </MenuItem>
            <Separator />
            <MenuItem Header="Fullscreen" InputGestureText="F11" Command="{Binding FullscreenCommand}">
                <MenuItem.Icon>
                    <fa:FontAwesome Icon="WindowMaximize" />
                </MenuItem.Icon>
            </MenuItem>
        </MenuItem>
    </Menu>
</UserControl>