<UserControl x:Class="Quizzly.Views.EndView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:Quizzly.Views" mc:Ignorable="d" Background="White" d:DesignHeight="450" d:DesignWidth="800" Height="auto" Width="auto" DataContext="{Binding EndViewModel}">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="0.5*" />
			<RowDefinition Height="0.5*" />
			<RowDefinition Height="0.5*" />
		</Grid.RowDefinitions>
		<Label Grid.Row="0" Content="GAME OVER" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center" />
		<TextBlock Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="30" Width="auto" Padding="5" Text="{Binding ResultText}" TextWrapping="Wrap" TextAlignment="Center" />
		<StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="2">
			<Button x:Name="back_button" Content="Back to menu" Margin="5" Padding="5" Click="back_button_Click" />
			<Button x:Name="playagain_button" Content="Play Again" Margin="5" Padding="5" Click="playagain_button_Click" />
			</StackPanel>
	</Grid>
</UserControl>
 