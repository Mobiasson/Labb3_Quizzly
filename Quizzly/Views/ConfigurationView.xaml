<UserControl x:Class="Quizzly.Views.ConfigurationView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:Quizzly.Views" xmlns:fa="http://schemas.fontawesome.io/icons/" mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800" DataContext="{Binding ConfigurationViewModel}">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Grid Grid.Row="1" Grid.Column="0">
			<Grid.RowDefinitions>
				<RowDefinition Height="auto" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
			</Grid.ColumnDefinitions>
			<DockPanel HorizontalAlignment="Stretch" LastChildFill="False" Margin="6 5 5 0">
				<TextBlock Text="{Binding ActivePack.Name}" Width="250" Margin="10 0 0 0" FontSize="15" DockPanel.Dock="Left" VerticalAlignment="Center" TextWrapping="Wrap" />
				<StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 0 10 0">
					<Button Content="Add" Margin="5 5 5 0" Padding="5" Command="{Binding AddQuestionCommand}" />
					<Button Content="Remove" Margin="0 5 0 0" Padding="5" Command="{Binding RemoveQuestionCommand}" />
				</StackPanel>
			</DockPanel>
			<ListBox x:Name="question_list" Grid.Row="1" Margin="15 5 15 15" BorderBrush="Black" BorderThickness="1" ItemsSource="{Binding ActivePack.Questions}" SelectedItem="{Binding ActivePack.SelectedQuestion, Mode=TwoWay}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<TextBlock TextWrapping="Wrap" MaxWidth="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=ActualWidth}">
							<Run Text="QUESTION: " FontWeight="Bold" />
							<Run Text="{Binding Query, Mode=OneWay}" /></TextBlock>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
		</Grid>
		<Grid Grid.Row="1" Grid.Column="1">
			<Grid.RowDefinitions>
				<RowDefinition Height="0.5*" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>
			<Label Grid.Row="0" Content="QUESTION:" FontSize="17" FontWeight="Bold" VerticalContentAlignment="Top" HorizontalAlignment="Left" Width="auto" Height="auto" Margin="25 0 0 0" />
			<TextBox x:Name="question_textbox" Grid.Row="0" Margin="30" BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Text="{Binding ActivePack.SelectedQuestion.Query,Mode=OneWay}" TextWrapping="Wrap" />
			<Grid Grid.Row="1">
				<Grid.RowDefinitions>
					<RowDefinition Height="auto" />
					<RowDefinition Height="auto" />
					<RowDefinition Height="auto" />
					<RowDefinition Height="auto" />
					<RowDefinition Height="*" />
				</Grid.RowDefinitions>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>
				<Grid Grid.Row="2" Margin="10 10 10 0">
					<Grid.RowDefinitions>
						<RowDefinition />
						<RowDefinition />
					</Grid.RowDefinitions>
					<Grid.ColumnDefinitions>
						<ColumnDefinition />
						<ColumnDefinition />
					</Grid.ColumnDefinitions>
					<StackPanel Grid.Row="0" Grid.Column="0" Margin="5">
						<Label Content="Correct Answer" />
						<TextBox x:Name="correct_textbox" Margin="0 2 0 0" BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Text="{Binding ActivePack.SelectedQuestion.CorrectAnswer, Mode=OneWay}" TextWrapping="Wrap" Background="LightGreen" />
					</StackPanel>
					<StackPanel Grid.Row="0" Grid.Column="1" Margin="5">
						<Label Content="Incorrect Answer 1" />
						<TextBox x:Name="incorrect1_textbox" Margin="0 2 0 0" BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Text="{Binding ActivePack.SelectedQuestion.IncorrectAnswers[0], Mode=OneWay}" TextWrapping="Wrap" Background="IndianRed" />
					</StackPanel>
					<StackPanel Grid.Row="1" Grid.Column="0" Margin="5">
						<Label Content="Incorrect Answer 2" />
						<TextBox x:Name="incorrect2_textbox" Margin="0 2 0 0" BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Text="{Binding ActivePack.SelectedQuestion.IncorrectAnswers[1], Mode=OneWay}" TextWrapping="Wrap" Background="IndianRed" />
					</StackPanel>
					<StackPanel Grid.Row="1" Grid.Column="1" Margin="5">
						<Label Content="Incorrect Answer 3" />
						<TextBox x:Name="incorrect3_textbox" Margin="0 2 0 0" BorderBrush="Black" BorderThickness="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Text="{Binding ActivePack.SelectedQuestion.IncorrectAnswers[2], Mode=OneWay}" TextWrapping="Wrap" Background="IndianRed" />
					</StackPanel>
				</Grid>
				<Button x:Name="AddQuestion_button" Grid.Row="3" Grid.Column="0" Width="auto" Height="auto" Margin="10" Padding="5" HorizontalAlignment="Left" VerticalAlignment="Center" Visibility="Hidden" Content="Add Question" Click="AddQuestion_button_Click" />
				<Button Content="PLAY" Grid.Row="3" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="15" Padding="12,6" FontSize="16" FontWeight="Bold" Command="{Binding DataContext.PlayCommand, RelativeSource={RelativeSource AncestorType=Window}}" />
				<StackPanel Grid.Row="4" Margin="15 5 15 15">
					<Label Content="Configurations:" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="15" FontWeight="Bold" />
					<TextBlock Width="auto" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding ActivePack.TimeLimitInSeconds, StringFormat='Time set to: {0} seconds'}" />
					<TextBlock Width="auto" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding QuestionCount, StringFormat='Questions: {0}'}" />
					<TextBlock Width="auto" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding ActivePack.Difficulty, StringFormat='Difficulty: {0}'}" />
				</StackPanel>
			</Grid>
		</Grid>
	</Grid>
</UserControl>